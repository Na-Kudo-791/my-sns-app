[project]
name = "my_sns_app"
version = "0.1.0"
requires-python = ">=3.8"
dependencies = [
    "flask",
    "python-dotenv",
    "click",
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

# --- 以下のセクションを追記 ---
[tool.setuptools.packages.find]
where = ["src"]